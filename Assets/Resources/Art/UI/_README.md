# UI 资源 (UI)

## 这个目录放什么
按钮、面板、图标等 UI 切图。

## 文件命名
- `btn_*.png` — 按钮
- `pnl_*.png` — 面板背景(可九宫格)
- `ico_*.png` — 图标

## 推荐尺寸
- 按钮:约 **300×96** 像素(@1x)
- 面板:按实际需求,九宫格资源用 **128×128** + 9-slice
- 图标:**64×64** 或 **96×96**

## 替换流程
直接覆盖 PNG 文件。九宫格资源在 Unity 里 `.png.meta` 调 Border 字段(美术不会调的话告诉开发协助)。
