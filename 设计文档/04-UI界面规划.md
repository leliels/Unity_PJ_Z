# 04 - UI 界面规划

> 本文档列出游戏中所有需要的 UI 界面，以及每个界面包含的元素。  
> 美术同学可以根据这个列表来设计 UI 原型和切图。

## 1. 界面总览

| 界面 | 所属场景 | 说明 |
|------|---------|------|
| 主菜单 | MainMenu | 游戏启动后的首页（模式选择） |
| 游戏主界面（HUD） | Game | 游戏过程中的信息展示 |
| 设置弹窗 | Game | 设置菜单（音效/音乐/震动/重新开始） |
| 游戏结束弹窗 | Game | 显示最终得分（无法继续时弹出） |

## 2. 各界面详细说明

### 2.1 主菜单

```
┌──────────────────────────────┐
│                              │
│         游戏 LOGO            │
│                              │
│       [ 开始游戏 ]           │
│       [   设置   ]           │
│       [   退出   ]           │
│                              │
│              v1.0            │
└──────────────────────────────┘
```

**元素清单：**
- 游戏 Logo / 标题文字
- "开始游戏"按钮
- "设置"按钮
- "退出"按钮（PC 端需要，移动端可省略）
- 版本号文字

### 2.2 模式选择

```text
┌──────────────────────────────┐
│                              │
│         游戏 LOGO            │
│                              │
│     [ 冒险模式 ]            │
│     [ 传统模式 ]            │
│                              │
│       [   设置   ]           │
│                              │
│              v1.0            │
└──────────────────────────────┘
```

**元素清单：**
- 游戏 Logo / 标题文字
- "冒险模式"按钮
- "传统模式"按钮
- "设置"按钮
- 版本号文字

### 2.3 游戏主界面（HUD）

```text
┌──────────────────────────────┐
│  ← 后退           ⚙ 设置   │
│            分数:320          │
│  ┌─────────────────────┐     │
│  │                     │     │
│  │                     │     │
│  │      棋 盘          │     │
│  │      8 × 8          │     │
│  │                     │     │
│  │                     │     │
│  └─────────────────────┘     │
│                              │
│   ┌───┐  ┌───┐  ┌───┐       │
│   │方块│  │方块│  │方块│       │
│   │ 1 │  │ 2 │  │ 3 │        │
│   └───┘  └───┘  └───┘       │
│       候 选 区               │
└──────────────────────────────┘
```

**元素清单：**
- 左上角：后退按钮（返回主菜单）
- 右上角：设置按钮（齿轮图标）
- 分数显示区域
- 中部棋盘区域（非 UI，是游戏对象）
- 底部候选区
  - 3 个方块候选位

### 2.4 设置弹窗

```text
┌──────────────────────┐
│        设置           │
│                       │
│   音乐  ━━━━○━━━     │
│   音效  ━━━━━━○━     │
│   震动    [ ✓ ]      │
│                       │
│     [ 重新开始 ]      │
│                       │
│      [ 关闭 ]         │
└──────────────────────┘
```

**元素清单：**
- 标题"设置"
- 音乐音量滑条
- 音效音量滑条
- 震动开关（开关按钮或复选框）
- "重新开始"按钮（重置当前游戏）
- "关闭"按钮（返回游戏）

### 2.5 游戏结束弹窗

```text
┌──────────────────────┐
│      游戏结束         │
│                       │
│      得分：680        │
│                       │
│     [ 重新开始 ]      │
│     [ 返回菜单 ]      │
└──────────────────────┘
```

**元素清单：**
- 标题"游戏结束"
- 最终得分显示
- "重新开始"按钮（重新开始当前模式游戏）
- "返回菜单"按钮（返回主菜单）

## 3. UI 技术说明

- 使用 Unity **uGUI** (Canvas + EventSystem)
- 所有弹窗使用同一个 Canvas，通过 **显示/隐藏 Panel** 切换
- Canvas 设置为 **Screen Space - Overlay** 模式
- Canvas Scaler 使用 **Scale With Screen Size**，参考分辨率 **1080 × 1920**（竖屏）
- **待确认：游戏是横屏还是竖屏？建议竖屏，更适合手机操作**

## 4. 美术同学需要提供的 UI 素材

| 素材类型 | 数量（估） | 说明 |
|---------|-----------|------|
| 按钮图片 | 2~3 种样式 | 主按钮、次按钮、小按钮 |
| 弹窗背景 | 1~2 种 | 圆角矩形面板 |
| 图标 | 5~8 个 | 齿轮、返回箭头、星星、锁、音乐、音效等 |
| 数字字体 | 1 套 | 分数显示用（可用免费字体） |
| 进度条 | 1 套 | 分数进度条底图 + 填充图 |

> 详细的美术规格见 [05-美术资源规范](05-美术资源规范.md)。
