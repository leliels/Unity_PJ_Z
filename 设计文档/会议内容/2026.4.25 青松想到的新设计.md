# 先写下来
- 排行榜
- 基础块块不能有超过4格宽度的。但首先，基础块块我们怎么手动来调整？请给出让策划手动调整的方法。
- FloatingScore现在我看它往上飘动的效果是通过 posy 产生的。此时我要调整它出现的位置，即它的初始位置。比如说，我要把它的高度在它一开始出现的时候就调得很高，我该怎么去设置？你得给我做这一套配置，让我去调试它出现的位置和效果。



# # Temp 4.25 1：27
- 我感觉候选区这三个整体对象之间的间距，你现在是按照它们的边缘来计算的吧？
我感觉这样不好，我希望以它们的坐标轴（整个对象的坐标轴）来进行计算，这样才是一个平均分布的状态，看上去才比较和谐。

- FloatingScore，需要像其他Prefab一样开放给我来设置字体、大小、位置等等

- 我现在调整 Bootstrap 上的 Cell Spacing 后得到的立即刷新结果，与我重新开始游戏后布局的间距依然不同。请继续检查这个问题，为什么会有不同？

# Temp 4.25 1：04
首先，候选区的可用内容（其内部每个块与块之间的间隔）会跟场景里的块布局间隔不一样。

我希望能统一由这个 cell spacing 来控制。因为如果下面看到的中间间隔不一样，感受是不统一的。尤其是当我拖动它准备往场景中去摆放的时候，它不是会放大一点吗？那个时候的状态和最终摆下去的状态，中间那个间隔完全不一样，带来的感觉非常奇怪。

另一个问题，我发现候选区有时候两个候选的内容之间可能会有重叠。我可能是把候选内容的尺寸放得过大了，这个我自己调整就好了。


还有个问题，就是当我把 Cell Spacing 进行设置后，它在现场改变的效果，和游戏重新开始（Restart）或第一次运行时中间间距的视觉效果看起来并不一样。

请帮我检查一下，是不是这两个状态下实际上赋值不同？并进行修正。


还有一个问题，关于分数显示格式：
我在这里进行了一些参数修改，但游戏中并没有实时产生变化。请检查原因，并让我能够在修改参数后，立即在游戏界面中进行刷新。


我还发现一个问题，就是候选区的三个对象，它们之间的间距我看并不是按照相同的间距来排布的。

第一个对象和第二个对象之间的间距，与第二个对象和第三个对象之间的间距，我目测并不一样。请告诉我现在这个间距是按什么来设定的，以及当我修改了 Candidate Spacing 之后，我影响的是什么效果？
