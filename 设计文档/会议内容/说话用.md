现在我们这个工程，我需要增加一个高亮提示：就是在已经触发了可消除状态的时候，这一排的方块是一个高亮状态。

# 讨论内容
排分 ： 改为 排分倍率；消除基本分 = 20，这里留个随机的口子，随机+-1，或者弄个随机区间；消除排数；排分倍率；Combo数，默认是1，重置也是1，不会归零；新增Combo数；Combo CD，默认为3，当为0时，Combo重置为1。每排 Combo 增长值，留一个。

有这么一些改动，我要对积分的各种概念和公式，以及积分流程进行一次重修订，然后，你按照这个东西来，先写一份新的积分规则，放在之前积分规则的文档下面，积分规则下面，新开一个部分来写吧。先把两个新的和旧的都放在文档里，因为接下来要进行修改，我觉得你可能需要知道，之前有啥，等我们修改完了的时候，再只留一下新的规则。好，然后我们下来说一下新的规则是怎么回事:

参数如下：
当前分A：就是玩家现在的总分数
消除基本分B：就是消除一排的话，最基础的一个底分儿。这个分我希望可以配置为区间随机数值，比如可以配置到19-21之间这种。默认值为20。
格子基础分C：就是一个格子自己由几个块块组成，就是几。这个不可配置，而是从格子本身直接获取。
格子基础分倍率D：用来跟C相乘，默认值为1，可配置。
消除排数L：触发消除时，看看横纵向一共消除了几排，就是几。
排分倍率R：根据消除排数，会有一个1、3、5、7、9的对应数值，也就是通过查表得到，这个数值表可配置。
Combo数M：是一个在游戏里会变动的数值，有时候会被重置为1，其最小值为1。
Combo增长值N：每消除 1 排增加多少 Combo 数，默认为1，可配置。
Combo CD值CCD：就是当消除后，还有几次机会来进行下一次消除来续上Combo，默认值为3，可配置。

规则说明：
- 如果没有触发消除
    - Combo CD值 CCD = CCD-1
    - 一旦CCD为0，则combo数M = 1，否则M不变
    - 当前分A不变
- 一旦触发消除
    - Combo数先进行计算：M = M + L*N
    - Combo CD值重置为默认值，比如配置了3，那就重置为3.
    - 分数计分公式为：A = A + C*D*R +  B*M*L*（L+1）



# 青松

## Codebuddy.md
说这个文件应该很少改动，只有架构上有变动才改。但我感觉之前绘画里每开发一个功能，都会去改一些东西，比如说，去加了几个新脚本，他就把这些脚本分别放到他说的目录里面。我觉得这个文件用这种方式改的话，以后频率有点太高了，而且，会显得这个结构不稳定，我觉得没必要这样。我不知道是什么原因促使我每次改了别的东西，Codebuddy里面就会进行大量变动，变动还很多，因为我在上一个对话里，他改了非常多次，每次就改一行，或者山一行，一直在改。我感觉这不太对，不知道哪儿触发了他会老去改这个，或者在哪里做的。每次修改点逻辑或项目有新改动，它就会变。我觉得不应该这样，这应该是个架构性的。架构应该是，如果我在现有架构里补充一些新文件，只要不是架构上的改变，就不用改这个文件。应该是这样，你帮我查一下为什么会这样，以及我应该在哪里修改防止这种情况。这个Codebuddy文件，是不是现在有冗余部分？把冗余部分都考虑清理一下，这是需要做的，嗯。然后再就是找出来，为什么会发生那么多次的反复写入修改这个文件，哪里规定了必须要改这儿。

我再补充一些说明，我发现每次创建完一个脚本，AI写了一些脚本后，马上就去改各个文档里关于这块需要更新和补充的东西。这种修改频率我觉得有点不太对，你应该把这次做的功能做完，我们把这几个功能产生的新资源和新结构等在文档上体现出来，再更新到文档里。应该是这样，你先把要改的东西都改完，而不是你改一次就对10个项目设计文档进行修改，再加一个新脚本后对10个文件进行一轮修改，这显然不是很合理，不是很经世致用，听起来也不太对，是吧？我觉得应该在库巴里面限定一下它的做法，就跟AI的制作流程。


## 【WIP】开放方块形状表的配置

## 待解决
- 编辑网格这里，行列数我改不动，也就无法测试是否能应用了。


### 已解决
将方块形状表的配置开放到引擎中，以策划友好的方式开放出来，让我们自己可以在编辑器模式下就配置这些可用方块的预设。
这块先出一个方案吧，看怎么样，呃，这个就是我们配置起来会比较舒适。因为你想我们并不是这个我们是美术，或者是一个很普通的策划。呃，你看用什么样的工具或者什么样的形式来配。因为它我得配那个块块的组合方式嘛，它它应该是有点图形化的，但你也可以用嗯非常简单的一个，比如说几乘几的格。每增加一个配置，你就以图形化的方式，让我去点格子，我就在格子里面点出一个形状来。然后然后另外我希望也有一个可以就是把所有已经配好的东西，在一个窗口里面显示的的的东西。总之这这个东西应该挺核心的。嗯，这个挺核心的，是一个配嗯。配置工具的开发，我认为某种程度上。但这个开发的方式以及我刚才说的这种形式是不是最合适的。你要帮我先调研呃分析一下，然后你给出我一个几个方案来，让我来选。

对你设计的几个点的确认：
1.好的。
2.接入运行时后，BlockSpawner Prefab 会多一个引用字段——这是做什么？需要我手动操作吗？还是你可以在计划表里列出来，你直接来通过MCP操作unity来完成这些必要的配置呢？
其他ok。
3.数据库我看可以加载不同的，那我想问一下，加载不同的可不可以做成配置文件里包含一组的，我们预设好的一组方块内容，如果另一个文件，我可以再配置另一组。可能里面已经配置了10种方块，这是A配置表啊，a database, 然后也许我另外创建了一个b database, b database里面有5种方块形状组合，是不是这样的呢？嗯，能不能这样？
我认为全部形状预览可以弄到另一个页签里面，比如弄到单独的页面，我浏览全部形状时，去那个页面浏览，你就不用在窗口里占用我编辑的空间了，不然显得挺复杂。我觉得，我不知道是不是我这么想的效果要好一点，而且你这个形状列表这儿显示的是名称，不直观呀，我光看名称，可能很难知道它具体是什么形状，你干脆就把行状列表和全部行状预览做到一起，你直接以它的形状保留一个名字，但最重要的还是我能直接看到它的形状长什么样，然后在一个可以滚动的区域内，我去浏览这些形状，可以打勾，这些形状要不要启用，前面有个勾选，勾选了就是启用，没勾选就是先不启用吧，这样也确实比较方便。但你这个预览和这个形状列表这两个区域，我觉得是可以分开，不是不分开，而是把它合到一块儿的就是只用一个东西展示就行了。而我前面说的单独开一个页面，我觉得也有必要，应该把它们以几乘几的分布，把现有的都排出来。排出来我们就一目了然知道现在都有啥。假如我们设定的组合非常多，有20种，你就用5×4的格子排布来展示所有排列组合，所有这个方块的预设。这块你就单独弄一个页面，把两块分成两个不同的页签，或者你把那块多按一个按钮，这个按钮打开显示所有组合模式，然后在那个模式下弄一个返回键之类的，你看这怎么设计好一点就怎么来，不是那么的讲究。
哦，另外我突然想到一个问题，因为我看到你示例配一个3格L在左下角，那我配的左下角最后生成的结果，它在候选区，摆放的位置会使那个对吗？因为它依然要出现在候选区的正中心，我不知道以现在的候选区设计，这么配置后，它是出现在候选区正中心还是左下角？如果是左下角，我就很担心后面怎么做了，因为我对于配一些别的，比如四个格子一二换一行34这种，不知道该怎么配了。所以你得先确保我画的所有形状，它都能都是以从左下角来来来配就行，然后他最终还能很居中的放到我的候选区，你你在代码里面也确认一下这个事儿。
其他这部分的设计是ok的。

对你需要我来确认的点的回答：
A. 这个尺寸，默认5×5，我觉得是够的，但是，这个编辑尺寸，我觉得你得给我设一个地方，让我能自己调的。但默认默认五十五五乘5没问题。但你，你给我一个调的手段，也配置在这个工具里面。也就是说，我可以调整几乘几，调整后编辑的界面就变成了几乘几的格子，我觉得目前先不设上限，或者你设一个上限就是16，是八的二倍。我不知道以后会不会扩展出一些好玩的玩法，把做得非常复杂而有趣，所以我先把这块能扩展的内容先留着，以配置的方式留着。
B. 我看你上面工具的界面设置里，已经有一个自动生成4个方向的功能了，我觉得可以啊，我觉得用这个做法2，我只配一个基础形状，工具点一下就自己生成旋转的几个方向，一共能直接生成3个，加起来一共4个，四种方向。
C. 我突然意识到一个问题，我不知道你说的权重是什么意思？这个权重跟游戏里面的什么机制相关，我不理解这个东西，嗯，我不理解这个东西，你你得把这个先跟我解释清楚.如果权重是出现的几率，那就可以先这么，如果是这样，我就用每个方向都要单独配置权重的方式来处理，因为我刚才B里面都说了，我的意思是点一下生成4个方向，他就自己给我基于这个生成另外4个方向的编辑好的配置，编辑好的配置直接加到数据库里面，加到当前正在编辑的数据配置文件里面，嗯，就行所以每个的那个权重，默认也是跟我创建第一个方向的给的权重是一样的就行。然后我自己手动手动去调其他几个方向的权重应该给多少。
D. 不强制连通。
E. 允许。

好了，我所有问题都回答完了，你就开始写正式方案文档，这个方案文档，你写完后，最好留一个档。算了，你先写出来，我看一下你新写的这个东西有没有别的问题，然后正式归档，你先不着急，我们先把新方案出出来，至于实现这些东西，你先不实现，我们还是先完善方案。




## 【完成】计算分数的逻辑拆分
请在02-游戏设计文档.md:60-60，看看现在的计分规则描述，是否有不合理的地方，然后梳理为更清晰的表述，让我审阅，让我自己先弄懂，要说的尽量清晰。

我看现在已经生成了一些RO config文件，这个文件我怎么确定现在调用的就是这个文件呢？应该说，有一个地方让我来配置，说我要用的就是这个config，而不是别的，因为我可以创建新的。比如我创建一个新的config，用来做另一个模式，用到时候我就可以调用另一个了吧？那我现在应该在哪里去做这个配置呢？就是说我我我现在要要用这一套config，还是说现在这个东西是写在硬编码里的？嗯，这块我希望是能也能开放出来的，具体用哪一个score config


## 其他修正
- [BlockSpawner] - 这个对象身上的脚本参数，看看是不是有冗余内容？
- FloatingScoreManager脚本，需要新建个对象来存放和生成里面的东西，单独管理。现在挂在GameCanvas上。



## 【完成】SceneBootstrap脚本冗余清理
- 棋盘布局模块的参数，我试了下都没用了好像，修改后没任何反应，有时候还会报错NullReferenceException: Object reference not set to an instance of an object
BlockPuzzle.Block.BlockSpawner.RelayoutCandidates () (at Assets/Scripts/Block/BlockSpawner.cs:222)
BlockPuzzle.Core.SceneBootstrap.<OnValidate>b__31_0 () (at Assets/Scripts/Core/SceneBootstrap.cs:422)
UnityEditor.EditorApplication.Internal_CallDelayFunctions () (at <6dd00658fb454e6fb4c06a416ab8eaa1>:0)
这些值我感觉已经没用了，是不是可以移除？
- 分数区域底板，以及分数显示格式，这两块的参数，是不是也没什么用了？因为现在分数不是是靠ScoreDisplay和HighScoreDisplay这两个东西来显示的嘛？如果确定无用就移除掉。

## 【完成】需要进行一次代码整理，更新文档
现在游戏的代码结构，我需要你帮我检查一下，看有没有冲突或冗余，需要优化的部分。先检查整体的代码结构，然后把最新的代码结构情况大体上跟我说一下，我再确认。

或者直接把最新的代码结构更新到技术架构文档里。技术架构文档中可能有我们还未实现的内容，那些不要改。但如果文档里原本的架构和命名已经不是项目最终实现的，而项目里已有对应实现，就以实际实现为基础，更新文档中已过期的内容。

## [完成]整理文档
呃，对于现在这个脚本来说啊，呃，不是这个工程来说，就是我为了让 AI 能够更容易读懂我这个项目的一些基础内容，就是不不会说是我新开一个对话框之后，这个 AI 就得重新便利一遍整个工程。我希望有一个嗯索引和整整体介绍的东西。你先看看。就现在呃我工程目录里面那个呃制作文档，就这种文档的写法，还有放在里面的这种位置什么的，它足不足以来给 code code body，你本身这个 agent 来比较容易的调用。或者说如果按照 codebody 的标准，我应该呃做一个什么样的工程说明和索引呢？我希望把这块呃整体进行一次迭代。然后你先帮我出一下方案。

【New】

OK，现在大的架构来说我觉得比之前好多了。不过，我在设计文档里看到有两个 04，有点冲突。请查看并修改一下，因为图集、图吉的做法其实是使用说明文档。而这部分我还没有处理。如果这块算是美术或操作用的说明文档，请在设计文档下新建一个目录来存放此类文件。把它移到新建的目录中。这是整体架构的考虑

然后，我需要你根据现在项目里的脚本和实际布局情况、实际资源布局情况，把最新的情况更新到所有对应的文档中。

请检查对应的文件和文档，例如已经完成的模块是否需要打上勾；对于新增的模块和功能，请把它们加进去。还有一些已经在架构上存在的资源，但文档中尚未体现，也请根据项目实际的脚本资源情况补充或修改到现有文档里。

## 【WIP】脚本整理
我需要把现有的脚本都梳理一下，并说明其中存在哪些问题。我现在主要说的是这个 Sense Bootstrap 脚本。这个脚本上，你首先帮我筛查一遍，哪些参数是有用的，哪些参数虽然暴露出来，但已经对实际游戏没有作用。这个你帮我理一个列表出来，然后我剩下的处理自行决定。

这些布局我觉得不应该放在脚本上，而是放在 Prefer 里。比如最高分和显示分数，整个区域应该是一个大的 prefab，我们在 prefab 中直接调整内部对象，如最高分模块、当前分模块以及装饰性元素，并在 prefab 里设置好位置。该 prefab 本身默认放在屏幕居中且稍微偏上一点的位置，先默认定一个位置吧。这里也应该拆出一个 prefab。我走的是 preweb 调用的，已经了解资源的操作方式。

我感觉这为了因为我现在目的是把整个这个脚本梳理一下嘛，这个梳理起来感觉点有点麻烦。因为现在里面有非常多的东西，嗯，我希望就是我我我说的这个我最终的最终目标哈，我希望这个脚本嗯只有极少量的，而且是影响游戏全局的一些呃。参数内容，然后其他一些嗯就是参数可调的参数要要少，能通过美术改资源就能调的，就交给美术去改这个 prefi 资源里面的这个相对位置什么的，应该这样，然后留下的这些参数。就比如说你刚才说的这个呃刚才弄了一个叫叫 loose candidate。类似这种这种用来测试临时测试的功能，或者说对游戏全局影响比较大，而不是只影响它的效果的这种东西嗯，都集中放在这这个区域里啊，这个脚本上，然后这个脚本最上面应该有一个栏目，整个栏目就是说我都要调用哪些 prefi 吧。然后把这些需要。调用的 pref 都填进来。对，这是是这个事，以及你要看一下，现在他调用的这些 pref 里面是不是有些是重复重复的。就比如说这个界面上调用了，但其实这个界面里的调用在实际游戏里并没有使用，而是比如说到那个包 der 呃 manage 这个里面不是也挂了一个脚本嘛，那个脚本里面也挂了几个 pref，嗯，就不要有重复的引用。如果说。这个东西这个逻辑的处理是在子脚本里面处理处理的那在这个总的脚本里就不要再去处理，应该把这个逻辑完全分开。如果总不是总脚本里面有还有一部分逻辑，那就应该把这个逻辑挪到那个子的脚本里面去。



## 【完成】效果相关-候选区
当我把候选区的东西拖上去的时候，这个过程中有两个效果问题。  
第一个问题是操作问题：我必须精准地点击候选区中物体的轮廓才能拖动它。但为了更好的玩家体验，手指只要在候选区内就应该能够拖动其中的物体。我建议在主脚本（boat strap）上添加一个开关来实现此功能。  

第二个问题是拖动后尺寸不正确：把物体拖到主界面或背景板上后，它的大小比真实大小还小一点。我想知道该如何调整，而不是通过代码修改。可能需要暴露相应的参数接口，或者直接修改对应 prefab 的尺寸。请告诉我具体是哪一个 prefab，以及该如何调整。

“[BoardManager]”这个对象，我看了一下，现在的逻辑稍微有点奇怪。必须把对象一开始就放到场景里，否则代码会自行再生成一个。假如场景中的对象本身是隐藏的，代码就不会使用它，相当于没有这个对象，于是代码会再生成一个，挂在上面的脚本会使用默认参数，导致效果全部错误。现在的做法，我希望改成这样：在 Bootstrap 脚本上应该有一个地方挂上 border manager的prefab

## 优化吸附手感
现在在最下面一行感觉，都拖到某个地方了，但还是没有进行吸附，手感奇怪。先把当前吸附的逻辑先告诉我，我会说出要修改的方向。


## 【已解决】candidate slot候选背景框问题
运行的时候，现在出现了比较多的报错，类似于丢失了某个东西的错误，需要先把这些报错解决。
我已经为候选区加上了背景框，但这个背景框不是固定的。拖动候选对象时，框会跟着一起移动，这显然不行。我要把框直接画在场景里，使其保持不动。也就是说，某些内容应当属于背景，而不属于候选区，和候选区的交互没有任何关系，只是背景的一部分。

因此，需要修改当前的结构——candidate slot 这个 prefab 以及对应代码的调用方式。把 bold、background 等元素移到整体 background 中，并将整个 background 开放为可定制的 prefab，这样就可以自由组合使用了。